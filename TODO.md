# TODO: Ensure Hero.jsx Background Image

- [x] Modify Hero.jsx to use CSS background-image instead of <img> tag for hero image.jpg
- [x] Remove the fallback emoji div since background will handle it
- [x] Build the project using `vite build`
- [x] Run `vite preview` to verify the background image displays correctly
- [x] Confirm changes are visible in the deployed frontend (user to push and redeploy)
